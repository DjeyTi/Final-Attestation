class PetShop implements Comand {
    @Override
    Object addAnimal(String name, String DoB, String animal) {
        if (animal.lowercase = "собака") 
            
    }
}

interface Comand {
    Object addAnimal(String name, Strign DoB, String animal);
}